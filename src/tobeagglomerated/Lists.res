let array1 = [1,2,3] 

/** lists aren't as fast as arrays? 
check out the JS output of this, it will how you how lists are nested 
the list has a head which is 1, and a tail that has a head which is 2 and 
a tail that has a head which is 3 and a tail that is 0? 
why? 

They are singluy-linked lists. This means that 


What are doubly-linked lists then? 
*/

let myList = list{1,2,3}